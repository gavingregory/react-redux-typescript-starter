/**
 * A map of T items, with a number as the key
 */
export interface NumberMap<T> { [s: number]: T; }

/**
 * A may of T items, with a string as the key
 */
export interface StringMap<T> { [s: string]: T; }
